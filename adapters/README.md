# 宿主平台 adapter 约定

核心流程不绑定 Claude Code、Codex、Hermes 或 OpenClaw。每个 adapter 只负责把已经核验的工具接入一个宿主。

新增 adapter 必须记录官方来源，并验证：宿主识别信号、支持的工具形态、用户/项目/全局作用域、实际写入位置、配置命令、重启/发现方式、卸载回滚、系统和 shell 差异，以及至少一项可复现测试。

缺少任一证据时不得写入平台专有命令；使用 generic 回退方案，并明确宿主接入尚未核验。
